{% extends 'tools/sidebar.html.twig' %}
{% import "tools/forms.html.twig" as forms %}
{% block child %}
    <div class="flex my-5">
        <label for="logo" class="text-slate-700 dark:text-slate-400">图片</label>
        <div class="flex-1 ml-4 flex flex-col relative">
            <div class="relative" x-data="{img: ''}">
                <button type="button" @click="$refs.logo.click()" class="absolute left-0 top-0 w-36 text-white py-1 rounded-md font-medium text-center !bg-primary border-2 border-solid !border-primary">上传图片</button>
                <input id="logo" x-ref="logo" accept=".jpg, .jpeg, .png, .webp" @change="img=window.URL.createObjectURL($el.files[0])" placeholder="上传logo" class="w-36 opacity-0 invisible" name="logo" value="选择图片" type="file" />
                <img class="mt-4 max-w-lg" x-show="img!=''" :src="img" />
            </div>
            <span class="text-sm text-slate-400 mt-2">限制最大为2M</span>
        </div>
    </div>
    <div class="flex my-5 justify-center">
        {{ forms.button('识别') }}
    </div>
    <div class="flex my-5">
        <label class="text-slate-700 dark:text-slate-400">结果</label>
        <div class="flex-1 ml-4">
            <textarea rows="10" x-model="data" class="block w-full rounded-md bg-slate-200 dark:bg-slate-900 dark:text-slate-300 border-transparent focus:bg-white focus:ring-0 focus:border-primary focus:dark:bg-slate-900/80"
                      placeholder="这里是识别之后结果">
            </textarea>
        </div>
    </div>
{% endblock %}
